# experiments/

Self-contained Python scripts, one per experiment. Run from project root:


```bash
python experiments/<name>.py
```

Files here are tracked in git; regeneratable outputs live in `results/` and `figures/` (not in git).
